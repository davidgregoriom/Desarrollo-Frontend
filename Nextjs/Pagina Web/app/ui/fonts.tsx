import { Lusitana, Montserrat} from 'next/font/google'

export const monserrat = Montserrat({subsets: ['latin']}) //Guay tener esto , lo descarga y lo tiene en el servidor

export const lusitana =  Lusitana({weight:['400','700'],subsets: ['latin']}) //Guay tener esto , lo descarga y lo tiene en el servidor
